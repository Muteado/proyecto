pass

'''

#Habría que llamar esta funcion en relación al turno

class IA:
    def lanzamientoRobot():
        if "Está en el limite izquierdo":
            angulo = randint(0,90)
            print(angulo)
            Textos.texto_pantalla_rect(angulo)
        if "Está en el limite derecho":
            angulo = randint(90,180)
            print(angulo)
            Textos.texto_pantalla_rect(angulo)
        velocidad = randint(0,limite velocidad)
        print(velocidad)
        Textos.texto_pantalla_rect(velocidad)

        bala = randint(0,2) #En realidad debe ser con el pos del mouse 
        #Llamar al movparabolico?
        #activar el espacio
'''